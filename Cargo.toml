[workspace]
members = ["crates/platform", "crates/shim"]
resolver = "3"

[workspace.package]
# The App's version comes from package.json; the crates are never published on their own.
version = "0.0.0"
edition = "2024"
rust-version = "1.97.1"
publish = false
